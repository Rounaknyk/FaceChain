# Demo input

Place a consented test image in this directory as `person.jpeg`. Demo images are
ignored by Git because they may contain biometric and other personal data.

The image should contain exactly one clear face and should already appear in a
public, Google-indexed social-media post if you want the reverse-search stage to
find a result.
